lab:
  message:
    local:
      enabled: false
    rocketmq:
      adapter:
        enabled: true
        consumers:
          lab.order-created-events:
            message-lab-order-primary:
              consume-mode: CONCURRENTLY
              consume-thread-number: 2
            message-lab-order-audit:
              consume-mode: CONCURRENTLY
              consume-thread-number: 2

# Official Apache RocketMQ Spring (sole native config source)
rocketmq:
  name-server: 127.0.0.1:9876
  producer:
    group: ${spring.application.name}-producer
