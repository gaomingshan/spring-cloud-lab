<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lab.commerce.mapper.CommerceOrderMapper">
    <insert id="reserve">
        INSERT IGNORE INTO t_commerce_order (request_id, user_id, product_id, count, money, status)
        VALUES (#{requestId}, #{userId}, #{productId}, #{count}, #{money}, 'PROCESSING')
    </insert>

    <select id="selectByRequestId" resultType="com.lab.commerce.entity.CommerceOrder">
        SELECT id, request_id, user_id, product_id, count, money, status, created_at
        FROM t_commerce_order
        WHERE request_id = #{requestId}
    </select>

    <update id="markCreated">
        UPDATE t_commerce_order
        SET status = 'CREATED'
        WHERE request_id = #{requestId}
          AND status = 'PROCESSING'
    </update>
</mapper>
